{
    "name": "console-trace"
  , "version": "0.1.0"
  , "description": "Adds a handy `trace` flag to the console object to prepend the file and line number"
  , "main": "./console-trace"
  , "dependencies": {
        "callsite": "*"
    }
}
